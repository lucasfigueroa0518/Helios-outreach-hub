/**
 * Retired with Embark Salesforce contacts dump.
 * Historical contact CSVs in archives.
 */
console.error(
  'check_contacts_coverage is retired — Helios no longer loads public.contacts.',
);
process.exit(1);
